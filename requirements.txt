# Core dependencies for RAG Component Extraction System
# ======================================================

# Vector database for component embeddings
chromadb>=0.4.0

# Sentence embeddings model
sentence-transformers>=2.2.0

# CLI framework for command-line tools
typer>=0.9.0

# HTTP client for LLM API calls (Ollama, cloud LLMs)
requests>=2.31.0

# Note: FastAPI server dependencies moved to server/requirements.txt
# Install server with: pip3 install -r server/requirements.txt

# Optional: Advanced features (currently not used, can be removed)
# openai>=1.0.0
# numpy>=1.21.0
# scikit-learn>=1.0.0
# nltk>=3.7
# python-dotenv>=0.19.0

# ============================
# FastAPI Server Dependencies
# ============================
# Install with: pip3 install -r server/requirements.txt

# FastAPI framework
fastapi>=0.104.0

# ASGI server
uvicorn[standard]>=0.24.0

# Data validation
pydantic>=2.0.0

# Required for CORS middleware (included with fastapi)
# Required for async operations (included with Python 3.7+)
